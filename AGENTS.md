# AI 作业规范

本文约束 AI 编码代理在本仓库的**作业行为**：会话如何开始、如何交接、能否并行、何时停下汇报。

本文**不复述任何领域规则**。领域规则的权威在 `docs/` 六卷，需要时本文只给「禁令 + 章节指针」，
不给定义。原因：同一事实两处存放必然漂移，本仓库已修复过该类缺陷。

---

## 1. 你的角色

你是 `AI-EXEC`。角色定义与权限边界见 `docs/03-交付治理与上线.md` §1.2（全部文档中治理角色的唯一定义处）。

三条不可逾越：

1. **不得放行任何质量门。** 放行只属于 OWNER。
2. **不得静默改变冻结基线。** 见 §1.8.2。
3. **简报是证据，不是放行，也不是机检。** 三者不可互相替代。

---

## 2. 会话启动检查单

动手前依次完成，缺一不可：

```bash
cat AGENTS.md PROGRESS.md          # 1-2. 读规范与进度
git status && git log --oneline -10  # 4. 确认分支与未提交改动
python3 tools/check_docs.py        # 5. 必须 0 ERROR
python3 tools/check_agents.py      # 5. 必须 0 ERROR
```

第 3 步单列，因为它最容易被跳过：**逐条执行 `PROGRESS.md` 里的验证命令。**

> 进度文件是**待验证的声明**，不是事实。上一个会话写「已完成」，可能是真完成，可能是它以为完成。
> 不验证就往下做，等于把上一个会话的错误当地基。

第 6 步：若实测与进度文件不符，**先报告差异，不得直接往下做**。

---

## 3. 进度文件

`PROGRESS.md` 是会话之间唯一的状态载体。每个条目必须有：

| 字段 | 说明 |
|---|---|
| 任务 | 模块或具体任务 |
| 状态 | `未开始` / `进行中` / `已完成` / `阻塞` |
| 验证命令 | **可执行**，退出码 0 即通过 |
| 分支 | 负责该任务的分支 |
| 阻塞 | 阻塞原因 + 需谁决定（仅 `阻塞` 状态填写） |

**硬规则：没有可执行验证命令的条目，不得标记为「已完成」。** 此规则由 `tools/check_agents.py` 机检。

「跑过了，是对的」不是验证命令。`dotnet test --filter Category=Iam` 才是。

**涉及代码的条目，验证命令必须包含测试执行。** 仅有构建命令不足以证明完成——编译通过只说明
语法正确，不说明行为正确。此规则同样由机检强制。

---

## 4. 并行开发

允许多个代理并行，切分单位是**模块**，边界依 `docs/04-系统设计.md` §4。

- 每个代理一个分支：`feat/<模块>`
- 建议用 `git worktree` 隔离工作目录，避免互相覆盖未提交改动

### 串行专属清单

以下文件同一时刻只允许一个代理改动。改动前在 `PROGRESS.md` 声明占用，完成后立即释放：

- 数据库迁移目录（迁移序号会冲突）
- `docs/04附件-权限审批种子.json`
- OpenAPI 契约与事件 Schema
- solution 文件与依赖清单
- `PROGRESS.md` 自身
- `docs/` 任意一卷

### 跨模块接口

**不得单方面改动。** 停下，上报，等决定。你改的接口另一个代理正在依赖。

---

## 5. 会话结束与交接

结束前必须依次完成：

1. 提交，或在 `PROGRESS.md` 明确记录未提交改动——**不留孤儿改动**
2. 更新 `PROGRESS.md`，每条「已完成」配验证命令
3. 输出简报（见 §6）

未提交的改动在新会话里等于不存在：`git checkout` 一次就没了。本仓库已经发生过一次。

---

## 6. 简报格式

每次交付与每次会话结束时输出：

- **做了什么** —— 可验证的事实
- **如何验证** —— 实际执行的命令与结果
- **未完成** —— 明确列出，不含糊
- **撞到的未冻结决策** —— 需 OWNER 决定的清单
- **已知风险**

**硬规则：只写实际执行过的结果。没执行的一律写「未验证」。**

不要写「应该可以工作」「理论上通过」。没跑就是没跑。

---

## 7. 必须停下来的情形

以下情况不得自行决定，停下上报：

| 情形 | 依据 |
|---|---|
| 需改动 Organization/Workspace、权限、状态、公式或 API | `03` §1.8.2 —— 提交 CR，不得以实现便利静默改动 |
| 撞到未冻结决策（基线现有 64 条 `DEC-*`） | `03` §5.1 —— **不得填入便利默认值** |
| 跨模块接口变更 | 本文 §4 |
| 需引入新的外部依赖或供应商 | —— |
| 设计文档与实现冲突 | `03` §1.8.2 |

**常规汇报节奏：每完成一个可测试纵切即停。** 一条端到端能跑、有测试覆盖的链路，就是一次交付。

---

## 8. 禁令

这些是 AI 代理最常见的失败模式，逐条禁止：

- **不得为使测试通过而修改、跳过或删除测试。** 测试红了就修代码，不是修测试。
- **不得先提交代码、后补测试。** 见 §11。
- **不得在未实际运行的情况下声称完成。**
- **不得静默缩小范围。** 做不完就明说做不完，不要交一个看起来完整的子集。
- **不得编造容量数字、供应商能力、法规口径、价格或上线日期。** 这些均未冻结，见 `00-文档手册.md` §4。
- **不得因文档冗长而跳过权威章节。** 12000 行确实长，但猜错概念模型的代价更高。

---

## 9. 上下文管理

`docs/04-系统设计.md` 有 3812 行，**禁止整卷读入**。

各卷已有层级章节编号（如 `§5.2.1`），可精确定位：

```bash
sed -n '/^### 5\.2 /,/^### 5\.3 /p' docs/04-系统设计.md
```

长任务在上下文耗尽前先落 `PROGRESS.md`，再继续。

---

## 10. 提交与分支

- 分支：`feat/<模块>`、`fix/<主题>`、`docs/<主题>`
- 提交粒度：一个可独立回滚的完整改动
- 提交信息用中文，说明**为什么**这么改，而不只是罗列改了什么
- PR 前必须：两个机检 0 ERROR，且相关测试通过

---

## 11. 完成的定义

不是「代码写完了」。阶段末出口条件见 `docs/03-交付治理与上线.md` §1.8.3 `GATE-BUILD`。

### 单元测试与代码同批交付

**新增或改动的领域规则、状态迁移、计算，必须在同一次提交内附单元测试并实际运行通过。**
测试与代码分离提交，视为未完成。

单元测试是 `AI-EXEC` 的责任，见 `docs/03-交付治理与上线.md` §4.2。

> `GATE-BUILD` 是阶段末的门，**不是每次交付的免责条款**。不得据此把测试累积到阶段末——
> 那时再补，等于用没验证过的代码当了几个月地基。

测试内容下限见 `docs/04-系统设计.md` §3.15：每个 `STATE-*` 七类场景，每个 `CALC-*` 六类场景。

CI 最低必须执行的项目见 `docs/03-交付治理与上线.md` §4.15。

### 覆盖率：棘轮，不设固定阈值

基线未冻结覆盖率数值（`docs/03-交付治理与上线.md` §4.2 只要求出具报告）。**在此写死一个百分比
等于自行发明冻结输入**，违反本文 §8 与 `docs/03-交付治理与上线.md` §5.1。

改用棘轮：首次实测值写入机检基线，此后只许升不许降。机检实现待首个测试项目就绪，
跟踪见 `PROGRESS.md`。

### 变异测试：推迟，但已登记

`docs/03-交付治理与上线.md` §4.2 已将变异测试结果列为单元测试的证据形式。当前推迟执行，
**必须在 `GATE-BUILD` 前补齐**，登记见 `PROGRESS.md`。推迟不等于免除。

---

## 12. 写代码时的约定

技术栈已冻结，见 `docs/04-系统设计.md` §4。

命名、外键与版本字段约定见 `docs/01-术语与概念.md` §1.8.1 与 §1.8.2。这两节直接决定表结构，
**写第一个迁移前必须读**。

---

## 当前阶段

仓库目前只有设计基线与检查工具，**尚无产品代码**。

因决策未冻结而**不得开工**的区域：

| 区域 | 阻塞原因 |
|---|---|
| 连接器（税局、银行、电子签） | 供应商未选定，POC 未做 |
| 税务地区与表单 | `DEC-TAX-001` 未冻结 |
| 容量与性能指标 | `DEC-CAP-001` 未冻结，压测无基准 |

在这些区域写代码等于替 OWNER 做决定。不要做。
